// Example with both guarded commands and ranking functions

const maxVal = 10

// Guarded commands (transitions)
[] x < maxVal -> x = x + 1
[] x >= maxVal -> x = 0

// Ranking function for state q0
rank(q0):
  [] x >= 0 && x < maxVal -> maxVal - x
  [] x >= maxVal -> 1
